{
    "build": {
      "builder": "nixpacks",
      "buildCommand": "cd demo && mvn install"
    },
    "deploy": {
      "startCommand": "cd demo && mvn spring-boot:run",
      "rootDirectory": "demo"
    }
  }
  